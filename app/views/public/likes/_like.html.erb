<% if tweet.favorited_by?(current_user) %>
  <%= link_to tweet_likes_path(tweet), method: :delete, remote: true do %>
    <div class="text-warning"><i class="fas fa-star"></i><%= tweet.likes.count %></div>
  <% end %>
<% else %>
  <%= link_to tweet_likes_path(tweet), method: :post, remote: true do %>
    <div class="text-warning"><i class="far fa-star"></i><%= tweet.likes.count %></div>
  <% end %>
<% end %>